🚀 Complete Setup Instructions
Step 1: Create Project Folder
bash

mkdir supply-chain-oracle
cd supply-chain-oracle

Step 2: Create All Files

Create these 3 files in the folder:

    requirements.txt
    train_model.py
    app.py

Step 3: Create Virtual Environment
bash

python -m venv venv

Step 4: Activate Virtual Environment
bash

# Windows
venv\Scripts\activate

# Mac/Linux
source venv/bin/activate

Step 5: Install Dependencies
bash

pip install -r requirements.txt

Step 6: TRAIN THE MODELS (Run this first!)
bash

python train_model.py

This will:

    ✅ Load your CSV data
    ✅ Preprocess and encode features
    ✅ Train XGBoost model
    ✅ Train LightGBM model
    ✅ Create ensemble model
    ✅ Save models to models/ folder
    ✅ Display accuracy reports

Step 7: Launch Web Application
bash

streamlit run app.py
```

### **Step 8: Use the Application**
1. Browser opens at `http://localhost:8501`
2. Upload your CSV file (same or new shipment data)
3. View predictions, SHAP values, and recommendations!

---

## 📂 Final Folder Structure
```
supply-chain-oracle/
├── venv/                           (virtual environment)
├── models/                         (created after training)
│   ├── xgb_model.pkl              (XGBoost model)
│   ├── lgb_model.pkl              (LightGBM model)
│   ├── le_carrier.pkl             (Carrier encoder)
│   ├── le_weather.pkl             (Weather encoder)
│   ├── le_traffic.pkl             (Traffic encoder)
│   └── metadata.json              (Model info)
├── train_model.py                 (Training script)
├── app.py                         (Web application)
└── requirements.txt               (Dependencies)

✨ What You Get
Training Phase (train_model.py):

    Trains XGBoost + LightGBM models on YOUR data
    Saves trained models for reuse
    Shows accuracy metrics and reports
    Only run this once (or when you have new training data)

Prediction Phase (app.py):

    Loads pre-trained models
    Upload ANY CSV to get instant predictions
    SHAP explanations for each shipment
    Smart recommendations
    Download results as CSV
